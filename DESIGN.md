# Calm Design

> 用红方技术打自己，拿回平静。
> 公网隐私审计 CLI：自动完成 OSINT 自查 → 身份关联图谱 → 断链优先级 → 复扫验证闭环。

---

## 1. 定位

| 项       | 内容                                                                                                                                                                    |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 一句话   | 公网隐私审计工具：扫出你在公开网络上的数字足迹，拼出关联链，告诉你剪断哪条链最便宜                                                                                      |
| 用户     | v1 只有 calm 自己（被骚扰后有真实需求的 zero 号用户）                                                                                                                   |
| 形态     | 单机 CLI，本地报告，无云端账号                                                                                                                                          |
| 竞品差异 | HIBP 只查泄露不查关联；SpiderFoot/Maltego 是渗透工程师的英文工具不给修复动作；国产隐私 App 查的是手机权限。calm 的空档：**中文语境 + 自我审计 + 关联链评分 + 修复闭环** |
| 哲学     | 和模型鉴真同一个母题——一个鉴模型，一个鉴自己。工具反打自己                                                                                                              |

## 2. 设计公理（所有决策的裁判）

1. **先为自己造**：每个 feature 过一票"上周的我会为此付钱吗"。
2. **被动源优先**：只查询、不抓取、不绕风控。技术洁癖换法律安全。
3. **诚实边界**：断不了的链报告里明说"不可修复"，绝不假装全能。
4. **断链 > 暴露**：暴露 100 条不可怕，1 条链才致命。输出围绕"链"。

## 3. 概念模型

```
Asset       资产：主动声明的标识符（手机号、邮箱、昵称、真名、头像）
Finding     发现：数据源挖到的碎片（"该昵称在贴吧有账号"，带 source/conf/raw_ref）
Link        关联边：两个 Finding/Asset 被确认的关联（同昵称/同邮箱/同头像/文本命中）
Chain       链：Asset → Finding → … → 敏感结论 的连通路径
Remediation 修复项：剪断某条边的一个具体操作（换头像/关入口/删帖/匿名化）
```

扫描 = `Finding × Finding → Link` 的实体对齐；报告 = `Chain → 最便宜的 Remediation`。

## 4. 数据源（已定案）

| #   | 源               | 接入                                  | 成本     | 说明                                                                                  |
| --- | ---------------- | ------------------------------------- | -------- | ------------------------------------------------------------------------------------- |
| D1  | 昵称碰撞搜索     | Brave Search API（免费档 2000 次/月） | $0       | 备选 SearXNG 自建                                                                     |
| D2  | 邮箱注册探针     | vendor `holehe`（MIT）                | $0       | 注册态探测，不抓内容；并发 1、间隔 1s                                                 |
| D3  | 泄露库查询       | HIBP API（key 约 $3.5/月）            | ~$3.5    | 只查不存，key 存本地配置                                                              |
| D4  | 用户名跨平台探针 | **vendor `maigret`（fork 裁剪）**     | $0       | 中文常用平台子集；头像/简介/注册时间全量落盘喂 Linker；礼貌延迟默认，激进模式自担风险 |
| D5  | 照片 EXIF        | 本地 `exiftool` / `piexif`            | $0       | 拖文件夹批量扫                                                                        |
| D6  | 修复知识库       | 自建 YAML，社区可贡献                 | 维护成本 | v1 内嵌 20-30 条平台指引                                                              |
| —   | 中文平台内容检索 | ❌ 不做爬虫                           | —        | 输出"人工任务"卡片：给关键词和搜索 URL，用户搜完贴回，工具做人机混合编排              |

## 5. 架构

```
assets.yaml ──→ ┌─────────────┐
                │ Collectors  │  D1-D5 asyncio 并行（semaphore 按源限流）
                └──────┬──────┘
                       ▼
                findings.jsonl   （append-only，含 source/confidence/raw_ref/timestamp）
                       ▼
                ┌─────────────┐
                │ Linker      │  规则对齐：同名昵称 / 同邮箱 / 同头像 hash /
                │ 实体对齐     │  文本中出现 Asset 值 → Link（类型 + 置信度）
                └──────┬──────┘
                       ▼
                ┌─────────────┐
                │ Scorer      │  暴露分 + 断链分（§6）
                └──────┬──────┘
                       ▼
                chains.json ──→ report.md / report.html
                                   diff（复扫对比，验证修复真的断了链）
```

Linker v1 纯规则（信号全是高置信硬信号），不上 ML；embedding 聚类预留给误报治理。

## 6. 评分模型

**暴露分** `Exposure = Σ (sensitivity × reach × conf)`

- sensitivity：手机号=3 / 真名+城市=3 / 邮箱=2 / 昵称=1 / 头像=1
- reach：公开=3 / 需登录=2 / 需付费库=1
- conf：确认=1.0 / 推断=0.5

**断链分** `CutScore = broken_chains × chain_weight / op_cost`

修一个动作断掉的链越多、链越重、操作越便宜 → 排越前。报告第一条永远是 CutScore 最高的动作。

## 7. 报告（Markdown + HTML）

```markdown
# calm 报告 — 2026-09-22

暴露面: ████████░░ 7.4/10 关联链: 4 条 可断点: 6 个

## 🔴 链 #1 — 手机号 → 知乎 → 学校（weight 9.1）

手机号(3.0) → [D1] 知乎昵称"xxx"(conf 0.5) → [D1] 回答命中"太原理工"(conf 1.0)
修复优先级：

1. 该回答匿名化 CutScore 9.1 成本 5min
2. 关闭知乎"通过手机号找到我" CutScore 4.2 成本 2min

## 🟡 不可修复项（诚实区）

身份证号已在历史泄露库（D3: 某酒店 2022 拖库）→ 建议接受；加固以身份证为验证的账号

## 📋 人工任务（工具不爬，你来当传感器）

去 Bing 搜 "你的手机号"，检查前 3 页是否有含定位的旧帖 → 结果贴回 calm task --submit
```

## 8. 工程结构

```
calm/
├── assets.yaml            # 资产清单（gitignore，含 HIBP key）
├── collectors/            # d1_search / d2_holehe / d3_hibp / d4_maigret / d5_exif
├── linker/                # rules.py / graph.py（networkx）
├── scorer/                # exposure.py / cutscore.py
├── kb/                    # remediation.yaml
├── report/                # markdown.py / html.py / diff.py / task.py
└── calm.py                # CLI: scan / report / diff / task
```

技术栈：Python + asyncio + httpx + networkx + Typer + rich。MVP 体量 < 3000 行。

## 9. Milestones

| 节点         | 内容                                                  | 验收                           |
| ------------ | ----------------------------------------------------- | ------------------------------ |
| M1（2-3 天） | D2 + D3 + D5，Linker 基础规则，Markdown 报告          | 跑自己出第一份报告             |
| M2（3-4 天） | D1 + D4（maigret），双评分 + CutScore 排序，HTML 报告 | 报告给出"第一条该做什么"       |
| M3（2 天）   | diff 复扫 + 人工任务卡片 + kb 扩至 30 平台            | 修复一周后复扫，看到链真的断了 |

## 10. 合规架构（写进代码，不是免责声明）

- ownership 验证：查手机号类资产需本机短信验证——没验证的号码功能层就查不了；
- 探针只探测注册态，不抓取、不存储第三方平台内容；
- 查询日志最小化，报告本地生成；
- calm 自己就是隐私产品的样板。

## 11. 明确不做

- ❌ 爬虫 / 反爬对抗 / 社工库接入（只走合法 API）
- ❌ 云端账号体系与报告托管
- ❌ 查询他人（输入即声明所有权）
- ❌ 威胁监控告警（订阅制产品的活，不是 CLI 的活）
- ❌ ML 关联（v1 规则够用，误报治理时再说）

## 12. 后续演进（记录在案，不进 MVP）

关联置信度升级（embedding 聚类降误报）→ Telegram bot 形态的"定期复扫提醒" → kb 社区化（平台设置变更众包）→ 作为 phus 生态的一个能力被编排调用（calm 是能力，不是被编排的附属——边界照旧）。

---

设计闭环。开工顺序建议：今晚先把 `assets.yaml` 填了 + M1 的 collector 骨架跑通，第一份报告出来的那一刻，这个工具就活了一半。需要我帮你把 M1 的 collector 接口定义（输入/输出/错误处理约定）先落到代码级吗？
